import React from 'react';
import styles from './index.module.scss';

type Props = {}

const About = (props: Props) => {
  return (
    <div className={styles.abort} id="about">施工中。。。👷</div>
  )
}

export default About